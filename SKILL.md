---
name: product-canon
description: Maintain a concise, user-authoritative product canon and turn confirmed product intent into one bounded, independently demonstrable implementation slice. Use when a product is starting, its core documents conflict or need correction, a new product or UX decision must be recorded, or a development workflow needs a current slice without generating a backlog of speculative specs.
---

# Product Canon

把用户已经确认的产品意志整理成稳定的产品核心文档，并输出一个当前可执行的产品切片。这个 Skill 与开发方法无关：输出可以交给 Speckit、TDD 或普通实现流程。

## 核心边界

- 用户当前明确决定优先于旧聊天、旧报告、测试、技能建议和局部实现。
- 产品合同/核心产品文档定义产品目标和永久边界；当前事实文档只记录源码与运行证据，不反过来改写产品目标。
- Skill 是整理过程，不是新的产品权威、任务系统、审批系统、Gate、调度器或数据库。
- 优先修正已有权威文档；只有现有文档确实没有合适位置时才新增文件。
- 默认只输出一个当前切片，不预先生成未来 Spec，不把技术层拆成多个产品 Spec。

## 工作流程

### 1. 识别请求类型

将请求归为以下一种：

- **建立**：从用户已确认的产品描述建立最小核心文档结构。
- **修正**：根据新的明确决定修正现有文档，并标记被取代的旧决定。
- **审计**：只读检查文档冲突、遗漏、重复和产品范围漂移。
- **选切片**：从当前产品目标中选出一个现在应实现的用户结果。

如果用户没有要求写入文件，先给出分析和拟修改位置，不擅自落盘。

### 2. 读取最小必要上下文

按顺序读取：

1. 项目的 `AGENTS.md` 或同等协作规则。
2. 用户本轮明确决定。
3. 项目列出的产品权威文档、界面/UX文档和当前事实文档。
4. 当前 Spec 的 `spec.md`、`plan.md`、`tasks.md`，仅在选择或修正切片时读取。

不要为了“完整”扫描无关源码、历史报告或全部 Spec。

### 3. 建立产品事实表

用下面的最小表格判断每个声明：

| 声明 | 来源 | 类型 | 当前处理 |
| --- | --- | --- | --- |
| 产品必须提供的用户结果 | 用户/产品合同 | 产品约束 | 保留 |
| 页面、交互、设置行为 | UX/界面权威 | 用户行为 | 保留或修正 |
| 当前代码、测试、运行结果 | 源码/命令 | 当前事实 | 不当作产品目标 |
| 旧方向、猜测、临时方案 | 聊天/报告/草稿 | 非权威 | 不自动继承 |

发现冲突时，优先采用用户本轮明确决定；如果冲突会改变产品范围、安全、真实账户、实盘、数据库迁移或用户体验主流程，列为一个短问题，不用技术细节代替产品决定。

### 4. 修正核心文档

只做最小、可追溯的文档修改：

- 把产品是什么、用户为什么需要、最终要达到什么结果写入产品合同。
- 把首页、主要工作区、常驻 AI 面板、账户切换和桌面设置写入产品界面/UX权威。
- 把自动链路、自适应范围、AI 与确定性系统的分工写入自治/架构权威。
- 把模拟、实盘、账户、订单、私域和运行证据边界写入运行/安全权威。
- 删除或合并重复、过时、过度工程化的描述；不要为了保留历史而复制一套新文档。

每次修改后说明：改了什么、取代了什么、仍未由代码或运行结果证明什么。

### 5. 输出当前切片简报

当前切片必须用用户结果描述，而不是技术模块描述：

```text
当前切片：<用户能看到或完成的一个结果>
用户：<谁使用它>
完成证据：<可运行的行为和验证方式>
包含：<本切片必须闭环的范围>
不包含：<明确留到以后或明确不做的内容>
对应权威：<核心文档路径>
交给开发流程：<Speckit / TDD / 普通实现>
```

切片规则：

- 一个切片必须可以独立演示或验收。
- 不按前端、后端、API、数据库、模型、测试或“收尾”拆切片。
- 如果拆开后任一部分无法单独产生用户结果，保持在同一个切片中。
- 只有新的独立用户结果、独立发布/授权边界或明确的产品范围变化，才创建新的 Spec。
- 缺失的实现细节回到当前切片的 plan/tasks/converge，不自动产生新 Spec。

## 与开发流程的关系

本 Skill 不要求使用 Speckit，也不修改 Speckit 核心。使用 Speckit 时，把“当前切片简报”作为 `specify` 的输入；使用 TDD 时，把同一简报作为测试和实现范围。Speckit 的 constitution 只承载开发原则，不复制完整产品合同。

不要把这个 Skill 的输出称为“Gate”。它只是开发前的产品上下文整理和范围检查；规格、方案、任务之间的交叉检查交给所选开发流程已有的工具。

## 停止条件

出现以下情况时停止扩大文档或切片，并报告具体缺口：

- 用户的两个明确决定互相冲突，且会改变产品方向。
- 请求把产品缩小成只有事件、新闻、单一页面或单一技术模块。
- 需要真实供应商、真实账户、实盘订单、持久自动化或数据库迁移的授权。
- 只能从测试、构建或文档推断产品已经完成，但缺少真实用户可见或运行证据。

## 最终输出格式

输出不超过五项：

1. 当前产品结论。
2. 文档冲突或遗漏。
3. 实际修改位置或拟修改位置。
4. 一个当前切片简报。
5. 仍需用户决定的事项；没有则写“无”。
